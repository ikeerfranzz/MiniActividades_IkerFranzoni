using UnityEngine;

public class ScriptE04: MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Vector3 vector;
    void Start()
    {
        Debug.Log(vector);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
